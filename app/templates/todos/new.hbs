<form id="new-todo-form">
    <div class="form-group">
        <lable>Title</lable>
        {{input type="text" class="form-control" value=title placeholder="Add todo..."}}
    </div>
    <div class="form-group">
        <lable>Due Date</lable>
        {{input type="date" class="form-control" value=date}}
    </div>
    <div class="form-group">
        <lable>Body</lable>
        {{textarea class="form-control" value=body placeholder="Todo Body..."}}
    </div>

    <button {{action "addTodo"}} class="btn btn-primary">Submit
    </button>
    {{#link-to 'todos' class="btn btn-default"}}
        Close
    {{/link-to}}

</form>